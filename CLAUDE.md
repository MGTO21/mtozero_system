# Mtozero Shop — نظام إدارة المخزون والمبيعات

سياق دائم للمشروع. اقرأ هذا الملف قبل أي تعديل.

## ما هو المشروع

نظام سحابي حقيقي **قيد الاستخدام اليومي** لمتجر أحذية وملابس (Mtozero Shop) في الأبيض، ولاية
شمال كردفان، السودان. المالك يبيع عبر واتساب وفيسبوك ومن المحل. النظام يحل محل التفتيش اليدوي
في الكراتين لمعرفة توفر المقاسات، ويتابع المبيعات والأرباح والديون من أي جهاز.

**ليس نموذجاً تجريبياً.** أي تعديل يجب أن يكون بجودة إنتاج: لا بيانات وهمية، لا شاشات نصف
مكتملة، ولا عمليات تكتب بيانات ناقصة.

## Stack

| الطبقة | التقنية |
|---|---|
| Frontend | Next.js 14 (App Router) + TypeScript (strict) |
| Styling | Tailwind CSS (dark mode بـ class) |
| Backend | Firebase — Firestore + Auth (بريد/كلمة مرور). **بدون Cloud Storage** |
| الصور | مصغّرة مضغوطة داخل مستند المنتج + نسخة كاملة اختيارية على Cloudinary |
| Offline | Firestore persistent cache (IndexedDB) + service worker لقشرة التطبيق |
| Deploy | Vercel |

اللغة: **واجهة المستخدم عربية RTL بالكامل. الكود والتعليقات بالإنجليزية.**

## بنية المشروع

```
src/
  app/
    layout.tsx            الخطوط + المزوّدات + تفعيل الثيم قبل أول رسم
    page.tsx              يحوّل إلى /dashboard
    login/                شاشة الدخول
    offline/              الصفحة التي يعرضها الـ service worker بدون شبكة
    (app)/                كل الشاشات المحمية خلف AppShell
      dashboard/ sell/ sales/ close/ customers/ debts/
      inventory/ shipments/ expenses/ reports/ activity/ team/ settings/
  components/
    layout/               AppShell (الشريط الجانبي + الشريط السفلي) + nav
    providers/            Auth / Theme / Toast
    ui/                   Button, Sheet, Feedback, Confirm, DateRange, PageHeader, Icons
    inventory/ sell/ sales/ dashboard/ pwa/
  lib/
    firebase.ts           تهيئة SDK + الكاش الدائم + التطبيق الثانوي لإنشاء الموظفين
    types.ts              نموذج البيانات كاملاً + التسميات العربية
    db/                   طبقة الوصول للبيانات (products, sales, expenses, users, activity)
    analytics.ts          كل التجميعات (ملخصات، أفضل المنتجات، الراكد، المدينون)
    format.ts             المال، الأرقام، التواريخ، أرقام واتساب
    invoice.ts            نص الفاتورة ورسالة تذكير الدين
    csv.ts                تصدير CSV/JSON بترميز يقرأه Excel عربياً
```

## قواعد معمارية غير قابلة للتفاوض

1. **كل عملية بيع تمر عبر `recordSale` في `src/lib/db/sales.ts` داخل Firestore transaction.**
   لا تخصم المخزون بـ `updateDoc` منفصل — هذا يسمح بالبيع الزائد عند استخدام جهازين معاً.
   **الفاتورة الواحدة تحمل أصنافاً متعددة** في `sale.items[]`، وكلها تُخصم في transaction
   **واحد**: إما تنجح الفاتورة كاملة أو لا يُكتب شيء. الأسطر تُجمَّع حسب `productId` أولاً
   فيُقرأ كل مستند منتج مرة واحدة فقط. **لا تُقسّم الفاتورة إلى عدة transactions.**
2. **لا حذف نهائي.** المنتجات تُؤرشف (`isArchived`)، المبيعات تُرجَع (`returns`) ولا تُحذف،
   وسجل النشاط للقراءة فقط. الاستثناء الوحيد: المصروفات، ويحذفها المالك فقط.
3. **الأرقام المشتقة تُحسب من مكان واحد**: على مستوى الصنف `itemNetQty` / `itemGross` /
   `itemCost`، وعلى مستوى الفاتورة `saleGross` / `saleTotal` / `saleDue` / `saleCost` —
   كلها في `sales.ts`، والتجميعات في `analytics.ts`. لا تكرّر الحساب داخل صفحة.
   **`Sale` لم يعد يحمل `productName` أو `size` أو `qty`** — استخدم `sale.items` أو
   `saleLabel(sale)` للعرض المختصر.
4. **`costPrice` و`productName` تُلتقط كـ snapshot وقت البيع** من داخل الـ transaction، حتى لا
   يغيّر تعديلُ سعرٍ لاحقاً أرباحَ الماضي.
5. **التنبيهات على مستوى المقاس وليس المنتج.** المالك يحتاج «مقاس 43 نفد» لا «هذا الموديل منخفض».
6. **كل عملية مؤثرة تكتب في `activityLog`** عبر `logActivity` — وهي fire-and-forget: فشلها
   لا يُرجع العملية التجارية.
7. **الصور**: `makeThumbnail` في `lib/image.ts` يضغط في المتصفح إلى ≤60KB ويُخزَّن الناتج في
   `thumbData` داخل مستند المنتج — لهذا تظهر الصور بدون إنترنت. رفع Cloudinary في
   `lib/cloudinary.ts` **best-effort**: فشله يحفظ المنتج بالمصغّرة ويعرض تنبيهاً، ولا يمنع
   الحفظ. لا تُدخل Firebase Storage — يتطلب خطة Blaze مدفوعة.
   اعرض الصورة دائماً عبر `productImage(product)` لا بقراءة الحقول مباشرة.
8. **الدفعات (lots) تعيش داخل مستند المنتج** في `sizes[].lots`. القاعدة الثابتة:
   `size.qty === sum(lots[].qty)` — و`reconcileSize` في `db/products.ts` هو **المكان الوحيد**
   المسموح له ببناء هذا الرقم. سبب وجود الدفعات داخل المستند: `recordSale` يظل يقرأ ويكتب
   مستنداً واحداً فيبقى الخصم ذرّياً بلا قراءات إضافية.
9. **الخصم بـ FIFO**: `consumeFifo` تأخذ من أقدم دفعة (`receivedAt`) أولاً، والبيع يسجّل
   الدفعات المستهلكة في `sale.lots`. الربح = السعر − تكلفة الدفعات الفعلية، لا متوسط تقديري.
   الإرجاع يعيد القطع لدفعتها الأصلية عبر `takeNewest` محافظاً على `receivedAt`.
10. **رصيد الإحالة يُخصم داخل نفس transaction البيع** (يُقرأ مستند العميل قبل أي كتابة).
    منح المكافأة للمُحيل يتم **خارجها** عبر `awardReferralIfDue` — لأن فشل المنح قابل للتكرار،
    بينما توسيع نطاق الـtransaction يزيد احتمال فشل خصم المخزون.
11. **الفواتير**: `lib/invoice-image.ts` يرسم على canvas (PNG للواتساب)، وصفحة `/invoice/[id]`
    للطباعة وحفظ PDF من المتصفح. لا تُدخل مكتبات html2canvas/jsPDF — الحل الحالي بلا تبعيات.
12. **`/close` يقيس النقد لا الإيراد.** النقد المستلم اليوم = مدفوعات مبيعات اليوم + تسديد ديون
    قديمة (`usePaymentsBetween`)، ولا يساوي `revenue` أبداً. لا تبسّطها إلى مجموع المبيعات.
13. **`restockPriority` تُرتّب بسرعة البيع لا بالكمية.** قائمة «منخفض» وحدها ليست قرار شراء —
    مقاس نفد ويُباع 8 في الشهر أهم من مقاس نفد ولم يُبع أبداً.

## الأدوار

- **owner**: كل شيء، بما فيه التقارير وسجل النشاط وإدارة الفريق وحذف المصروفات.
- **employee**: البيع، المخزون، المبيعات، الديون، المصروفات (إضافة فقط). لا يرى التقارير ولا
  سجل النشاط. يرى هامش الربح فقط إذا فعّل له المالك `canSeeProfit`.

الحماية بثلاث طبقات: إخفاء في `nav.ts`، تحويل في `AppShell`، ومنع في `firestore.rules`.

> ⚠️ **أي تغيير في شكل مستند يجب أن يمرّ على `firestore.rules` في نفس اللحظة.** القواعد
> تذكر أسماء حقول بعينها، وقراءة حقل غير موجود ترفض القاعدة كاملة — فيتحوّل تغيير في
> النموذج إلى «ليست لديك صلاحية» لا علاقة لها بالصلاحيات. حدث هذا فعلاً عند تحويل البيع
> إلى سلة: توقف تسديد الديون والإرجاع معاً. **وتذكّر أن نشر القواعد خطوة يدوية منفصلة.**

أول حساب يدخل بالبريد الموجود في `NEXT_PUBLIC_OWNER_EMAIL` يُمنح دور المالك تلقائياً — وهذه
القيمة يجب أن تطابق `ownerEmail()` داخل `firestore.rules`.

## الهوية البصرية

- **اللوغو**: MTOZERO — تدرّج أزرق سماوي `#1B9BE8` ← بنفسجي `#7E33D4`. مبني SVG في
  `components/layout/Brand.tsx` (لا PNG). التدرّج **للعلامة فقط** — ممنوع في الخلفيات.
- لون التفاعل: `#E84B8A` (فوشيا) — `brand-500`. الأزرار والأسعار والحالة المختارة.
  اختير عمداً لأنه يتفوّق على أزرق اللوغو في التباين فوق الخلفية شبه السوداء.
- لون الهوية: `accent-500` (أزرق اللوغو) — لكل ما يتحدث عن العلامة لا عن إجراء.
- الخلفية: near-black محايد الحرارة (`ink-950` / `ink-900`)، ليست بنفسجية. الوضع الليلي افتراضي.
- الخطوط: **Cairo** (700–900) للعناوين، **IBM Plex Sans Arabic** للنصوص والأرقام.
- كل رقم مالي أو كمية يأخذ `tnum` + `font-display` بحجم كبير — النظام يُقرأ أثناء البيع لا للتأمل.
- الأيقونات في `ui/Icons.tsx` لكل واحدة معنى واحد. المقاس المتوفر والمقاس النافذ لهما **شكلان
  مختلفان** (متصل مقابل متقطّع) لا لونان فقط.
- ممنوع: gradients بنفسجية، بطاقات إحصائية متطابقة، `shadow-xl`/`rounded-2xl` بلا سبب.

**أهم شاشة في النظام هي `/sell`.** أي تعديل عليها يجب ألا يزيد عدد النقرات: منتج ← مقاس ← تأكيد.

## أوامر

```bash
npm run dev        # تشغيل محلي
npm run build      # بناء الإنتاج
npm run typecheck  # tsc --noEmit
npm run lint
node scripts/generate-icons.mjs   # إعادة توليد أيقونات PWA
```

الإعداد الكامل لـ Firebase في `docs/SETUP.md`. حالات الاختبار الحرجة في `docs/TESTING.md`.

## متبقٍ (Phase 2)

مذكور بالتفصيل في نهاية `README.md` — أهمه: Barcode/QR، النسخ الاحتياطي التلقائي الأسبوعي،
وصفحة طلبات التوريد.
